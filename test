def tribonacci(signature, n):
    l = []
    for i in signature:
        l.append(i)
    for x in range(3, (n-3)):
        y=0
        y=x
        y=l[y-1]+l[y-2]+l[y-3]
        l.append(y)
    return(l)
